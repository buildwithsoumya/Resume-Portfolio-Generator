You are an expert portfolio website generator.
Generate a complete single-page portfolio website.

STRICT RULES:
- Use only resume data
- Do not invent projects
- Do not invent companies
- Do not invent skills
- Do not invent experience
- Do not invent certifications
If information is missing, keep sections minimal.

HTML REQUIREMENTS:
- Return ONLY the raw HTML document. No markdown formatting. No explanations.
- Embedded CSS
- Responsive
- Dark/light mode
- Semantic HTML
- Clean structure

STYLE GUIDANCE for {portfolio_style}:
{style_guidance}

CRITICAL HTML STRUCTURE REQUIREMENTS for Editor:
You MUST add the following attributes to the corresponding HTML elements so the frontend can edit them:
- `data-editable="name"` for the user's name
- `data-editable="headline"` for their headline/title
- `data-editable="about"` for their summary/about text
- `data-editable="contact"` for their contact info container
- `data-list="skills"` for the container element (like <ul> or <div>) that holds skill items
- `data-list="projects"` for the container element that holds project items
- `data-list="experience"` for the container element that holds experience items
- `data-list="education"` for the container element that holds education items
Each item inside a `data-list` container should be a clean, distinct child element so it can be cloned or removed.

Portfolio Plan:
{portfolio_plan}

Resume Data:
{resume_data}

Return ONLY HTML. Start with <!DOCTYPE html>.
